# 🚀 Итоговая работа по Binary Analysis 🚀

## 🌟 Введение

Данный документ представляет собой итоговый отчет по комплексу практических работ, выполненных в рамках подготовки к сессии по Binary Analysis. Отчет демонстрирует освоенные навыки и глубокое понимание в области анализа исполняемых файлов, реверс-инжиниринга, поиска уязвимостей и методов защиты программного обеспечения. Каждое задание отражает ключевые этапы исследования и практического применения современных техник и инструментов в сфере кибербезопасности.

<hr/>

## 📚 Оглавление

1.  [Задание 1: Анализ файлов ELF и PE](#задание-1-анализ-файлов-elf-и-pe)
2.  [Задание 2: Дизассемблирование сложного бинарного файла](#задание-2-дизассемблирование-сложного-бинарного-файла)
3.  [Задание 3: Динамический анализ вредоносного ПО](#задание-3-динамический-анализ-вредоносного-по)
4.  [Задание 4: Автоматизация анализа с помощью IDAPython](#задание-4-автоматизация-анализа-с-помощью-idapython)
5.  [Задание 5: Обход анти-отладочных механизмов](#задание-5-обход-анти-отладочных-механизмов)
6.  [Задание 6: Анализ упаковщиков — упаковка и ручная распаковка с UPX](#задание-6-анализ-упаковщиков--упаковка-и-ручная-распаковка-с-upx)
7.  [Задание 7: Обфускация кода на C/C++](#задание-7-обфускация-кода-на-cc)
8.  [Задание 8: Разработка простого эксплойта](#задание-8-разработка-простого-эксплойта)

<hr/>

## 📝 Детализация Заданий

### Задание 1: Анализ файлов ELF и PE

**🎯 Цель:** Изучить структуры исполняемых файлов ELF (Linux) и PE (Windows), понять их заголовки и написать скрипт на Python для извлечения ключевой информации.

**🛠️ Основные задачи:**
*   Проанализировать заголовки и структуры файлов ELF и PE с использованием утилит `readelf`, `objdump` (для ELF) и `dumpbin`, `PE-Bear` (для PE).
*   Определить тип файла, архитектуру, секции (для ELF) и таблицы импорта/экспорта (для PE).
*   Написать скрипт на Python с использованием библиотеки `lief` для автоматического анализа и извлечения информации из заголовков, секций (ELF) и таблицы импорта (PE).
*   Протестировать скрипт на реальных файлах в Linux и Windows.
*   Рассмотреть возможности модификации файлов (добавление секций, изменение точки входа) и поиска вредоносных признаков.

**🔑 Ключевые концепции:**
  * Форматы ELF и PE
  * Заголовки файлов
  * Секции
  * Таблицы импорта/экспорта
  * Статический анализ
  * Библиотека `lief`

**🔍 Что было сделано:**
* Изучена структура ELF и PE файлов через документацию и практическое исследование
* Написан скрипт на Python (файл `file_analyzer.py`) для автоматизации извлечения информации из исполняемых файлов
* Проанализированы реальные бинарные файлы: `/bin/ls` (ELF) и `notepad.exe` (PE)
* Проведены эксперименты по модификации заголовков и секций с целью изучения влияния на работу файлов

**🛠 Как было сделано:**
* Применены утилиты `readelf -h`, `readelf -S`, `objdump -d` для анализа ELF-файлов в Linux
* Использованы инструменты `PE-Bear` и `CFF Explorer` для анализа структуры PE-файлов в Windows
* Разработан Python-скрипт с библиотекой `lief` для кросс-платформенного анализа бинарных файлов
* Написаны функции для автоматического обнаружения потенциально опасных импортируемых функций
* Реализован механизм сравнения секций для выявления аномалий в структуре файла

**📊 Результаты:**
* Успешно создан скрипт, способный анализировать ELF и PE файлы с выводом подробной информации о структуре
* Обнаружены и задокументированы различия между форматами файлов и их особенности
* Выявлены критические секции, подверженные модификации злоумышленниками
* Составлена таблица сравнения популярных утилит для анализа исполняемых файлов
* Создана документация по использованию разработанного инструмента

### Задание 2: Дизассемблирование сложного бинарного файла

**🎯 Цель:** Проанализировать защищенный бинарный файл с использованием дизассемблеров.

**🛠️ Основные задачи:**
1.  Дизассемблировать предоставленный бинарный файл с помощью **IDA Pro**, **Ghidra** или **Radare2**.
2.  Определить основную логику работы программы.
3.  Выявить использованные защитные механизмы (обфускация, упаковка, анти-отладка).
4.  Обойти защиту и восстановить исходный алгоритм программы.
5.  Изменить поведение программы (например, убрать проверку лицензии).
6.  Дополнительно выбрать и дизассемблировать два бинарных файла из репозиториев.

**🔑 Ключевые концепции:**
  * Дизассемблирование
  * Обратная инженерия
  * Обфускация
  * Упаковщики
  * Анти-отладочные приемы
  * `IDA Pro`, `Ghidra`, `Radare2`

**🔍 Что было сделано:**
* Проведен анализ защищенного бинарного файла `crackme_v2.exe` со сложными механизмами защиты
* Выполнено дизассемблирование в трех средах: IDA Pro, Ghidra и Radare2 для сравнения возможностей
* Восстановлен алгоритм генерации и проверки лицензионного ключа
* Создан патч для обхода защиты и модификации поведения программы
* Дополнительно проанализированы два файла с разных источников: `sample1` и `sample2`

**🛠 Как было сделано:**
* Использована IDA Pro для начального дизассемблирования и изучения потока выполнения
* Выявлены антиотладочные механизмы с помощью статического и динамического анализа
* Применены плагины для IDA Pro (FindCrypt, Keypatch) для упрощения анализа
* Восстановлены строки и алгоритмы с использованием дедуктивного подхода и патчинга
* Написаны скрипты на Python для автоматизации извлечения критических данных
* Построены графы вызовов и блок-схемы алгоритмов для визуализации логики

**📊 Результаты:**
* Определен полный алгоритм работы защищенной программы и его документирование
* Обнаружены все используемые методы защиты, включая обфускацию и анти-отладку
* Успешно модифицировано поведение программы путем создания патча (файл `crackme_patched.exe`)
* Создан генератор валидных лицензионных ключей на основе восстановленного алгоритма
* Подготовлена сравнительная таблица возможностей дизассемблеров с рекомендациями по применению

### Задание 3: Динамический анализ вредоносного ПО

**🎯 Цель:** Изучить продвинутые техники динамического анализа вредоносного ПО, включая настройку сложных песочниц, обход анти-отладочных техник, трассировку API и анализ сетевого взаимодействия.

**🛠️ Основные задачи:**
*   **Настройка сложной песочницы:** Использовать `Cuckoo Sandbox`, `API Monitor`, `FakeNet-NG` для анализа `eicar.com` и обхода детектирования ВМ.
*   **Анализ самоудаляющегося ПО:** Проанализировать `qbot.exe`, отследить процесс удаления с помощью `Process Monitor` и извлечь полезную нагрузку с помощью `Volatility`.
*   **Обход анти-отладочных техник:** Идентифицировать и модифицировать анти-отладочные вызовы (например, `IsDebuggerPresent`) в `upx_sample.exe` с помощью `x64dbg`/`WinDbg`.
*   **Трассировка API и извлечение полезной нагрузки:** Отследить вызовы API в `backdoor.exe` с помощью `API Monitor`, извлечь динамически загруженные модули с помощью `Scylla`.
*   **Реверс-инжиниринг алгоритмов:** Дизассемблировать `wannacry_sample.exe` с помощью `IDA Pro`, идентифицировать алгоритмы шифрования и разработать скрипт для дешифровки строк.
*   **Анализ сетевой коммуникации:** Перехватить и проанализировать сетевой трафик `redline.exe` с помощью `FakeNet-NG` и `Wireshark`, разработать тестовый командный сервер.

**🔑 Ключевые концепции:**
  * Динамический анализ
  * Песочница (Sandbox)
  * Анти-отладка
  * Трассировка API
  * Извлечение полезной нагрузки
  * Анализ сетевого трафика
  * `Volatility`, `Process Monitor`, `x64dbg`, `IDA Pro`, `Wireshark`, `FakeNet-NG`

**🔍 Что было сделано:**
* Настроена сложная песочница с комбинацией инструментов для безопасного анализа вредоносного ПО
* Проведен комплексный анализ образцов вредоносного кода разных типов: самоудаляющееся ПО, шифровальщик, бэкдор
* Разработаны методики обхода защитных механизмов в анализируемых образцах
* Создан эмулятор командного сервера для анализа сетевого взаимодействия вредоносного ПО
* Извлечена и проанализирована полезная нагрузка из образцов

**🛠 Как было сделано:**
* Установлена и настроена Cuckoo Sandbox с изолированной средой для анализа
* Настроены инструменты API Monitor и FakeNet-NG для перехвата системных вызовов и сетевого трафика
* Применен Process Monitor для отслеживания активности самоудаляющихся образцов
* Использован Volatility для анализа дампов памяти и извлечения скрытых компонентов
* Модифицированы образцы для обхода анти-отладочной защиты с помощью x64dbg
* Проведен анализ сетевых протоколов взаимодействия с C&C серверами при помощи Wireshark

**📊 Результаты:**
* Создана документированная методика настройки безопасной аналитической среды
* Извлечены и документированы конфигурации всех анализируемых образцов вредоносного ПО
* Восстановлены алгоритмы шифрования и разработаны инструменты для дешифровки
* Построены карты сетевого взаимодействия для каждого из образцов
* Подготовлена сравнительная таблица характеристик исследованных образцов
* Создан набор инструментов и скриптов для автоматизации анализа схожих образцов

### Задание 4: Автоматизация анализа с помощью IDAPython

**🎯 Цель:** Освоить автоматизацию обратной инженерии с использованием IDAPython в IDA Pro.

**🛠️ Основные задачи:**
*   **Поиск вызовов конкретной функции:** Найти все места вызова заданной функции (например, `strcpy`) с помощью `LocByName` и `CodeRefsTo`.
*   **Извлечение всех функций:** Получить список всех функций, их адресов и размеров с помощью `Functions`, `GetFunctionName`, `GetFunctionAttr`.
*   **Поиск опасных функций:** Идентифицировать использование потенциально уязвимых функций (`strcpy`, `system`, `gets`, `memcpy`).
*   **Извлечение строк:** Найти и извлечь все текстовые строки из бинарного файла с помощью `Strings`.
*   **Дополнительные задачи:** Автоматическая аннотация функций, анализ зависимостей функций (построение графа вызовов), обход анти-отладочных техник.

**🔑 Ключевые концепции:**
  * IDAPython
  * Автоматизация реверс-инжиниринга
  * IDA Pro API (`idautils`, `idc`, `idaapi`)
  * Поиск ссылок на код
  * Анализ функций
  * Извлечение строк

**🔍 Что было сделано:**
* Разработан комплексный скрипт на IDAPython для автоматизации рутинных задач анализа
* Создан набор функций для поиска потенциально опасных вызовов функций
* Реализована автоматическая аннотация и документирование функций в базе данных IDA
* Разработан инструмент для построения и визуализации графа вызовов функций
* Внедрены механизмы извлечения и анализа строковых данных из бинарного файла

**🛠 Как было сделано:**
* Изучен API IDAPython через документацию и примеры
* Написаны модульные скрипты для решения отдельных задач анализа
* Использованы функции IDAPython (`LocByName`, `CodeRefsTo`, `Functions`) для навигации по коду
* Применены методы автоматического поиска и идентификации уязвимых паттернов в коде
* Реализован экспорт данных в форматы JSON и CSV для дальнейшего анализа
* Интегрированы визуальные элементы для улучшения представления результатов в IDA Pro

**📊 Результаты:**
* Создан набор скриптов для автоматизации анализа (`vuln_finder.py`, `string_analyzer.py`, `function_mapper.py`)
* Значительно сокращено время на рутинные операции поиска потенциальных уязвимостей
* Успешно протестированы скрипты на нескольких бинарных файлах разной сложности
* Реализована автоматическая система отчетности с выгрузкой в HTML и PDF форматы
* Создан удобный интерфейс для запуска скриптов в IDA Pro с настраиваемыми параметрами
* Опубликованы полученные скрипты на GitHub для доступа сообщества

### Задание 5: Обход анти-отладочных механизмов

**🎯 Цель:** Научиться идентифицировать и обходить анти-отладочные техники, использовать отладчик для анализа и разрабатывать бинарные патчи для отключения защит.

**🛠️ Основные задачи:**
*   Изучить распространенные анти-отладочные техники (`IsDebuggerPresent`, `CheckRemoteDebuggerPresent`, `NtQueryInformationProcess`, SEH, INT3, проверки времени, регистры DRx).
*   Использовать инструменты (`x64dbg`, `PE-bear`, `CFF Explorer`, `IDA`/`Ghidra`, `ScyllaHide`) для анализа и отладки.
*   На примере простой программы с `IsDebuggerPresent` обойти защиту в отладчике (`x64dbg`), изменив результат вызова или поток выполнения.
*   Создать перманентный патч для бинарного файла, отключающий анти-отладочную проверку, путем модификации инструкций (например, замена `CALL` на `MOV EAX, 0` или использование `NOP`).
*   Применить полученные навыки для анализа и патчинга защищенного бинарного файла (например, с `crackmes.one`).

**🔑 Ключевые концепции:**
  * Анти-отладка
  * Отладка (debugging)
  * Бинарный патчинг
  * `x64dbg`
  * `IsDebuggerPresent`, `CheckRemoteDebuggerPresent`, `NtQueryInformationProcess`
  * Точки останова
  * Модификация кода в памяти и в файле

**🔍 Что было сделано:**
* Исследованы различные анти-отладочные техники и методы их обнаружения
* Разработана тестовая программа с несколькими уровнями защиты от отладки
* Созданы методики и инструменты для обхода каждого типа защиты
* Написаны патчи для нейтрализации анти-отладочных механизмов
* Проведен комплексный анализ защищенного бинарного файла с crackmes.one

**🛠 Как было сделано:**
* Изучены механизмы работы функций `IsDebuggerPresent`, `CheckRemoteDebuggerPresent` и `NtQueryInformationProcess`
* Использован отладчик x64dbg с плагином ScyllaHide для динамического патчинга в памяти
* Применен IDA Pro для статического анализа защитных механизмов
* Разработаны скрипты для автоматического обнаружения анти-отладочных приемов
* Созданы перманентные патчи с использованием hex-редакторов и PE-редакторов
* Протестированы разные подходы к обходу временных проверок и проверок на виртуальную среду

**📊 Результаты:**
* Составлена база знаний по различным анти-отладочным техникам и методам их обхода
* Разработаны инструменты для автоматического обнаружения и нейтрализации защит
* Успешно обойдены все защитные механизмы в тестовых программах
* Создан рабочий набор патчей для распространенных анти-отладочных приемов
* Составлено подробное руководство по обходу защит с пошаговыми инструкциями
* Предложены улучшения для инструмента ScyllaHide на основе полученного опыта

### Задание 6: Анализ упаковщиков — упаковка и ручная распаковка с UPX

**🎯 Цель:** Понять принцип работы упаковщиков исполняемых файлов, использовать упаковщик UPX для сжатия исполняемого файла и вручную распаковать его с помощью отладчика.

**🛠️ Основные задачи:**
1.  Написать и скомпилировать простую программу на C (`hello.c`).
2.  Упаковать исполняемый файл с помощью утилиты `upx`.
3.  Проанализировать упакованный файл с помощью PE-анализатора (обнаружить секции `UPX0`, `UPX1`).
4.  Открыть упакованный файл в отладчике (`x64dbg`) и протрассировать выполнение кода-заглушки (stub) UPX до достижения точки оригинального входа (OEP - Original Entry Point).
5.  Сделать дамп памяти процесса в момент, когда оригинальный код распакован.
6.  Восстановить таблицу импорта (IAT) с помощью утилит типа `Scylla` или `Import REConstructor`.
7.  Сохранить распакованный дамп как исполняемый файл и проверить его работоспособность.

**🔑 Ключевые концепции:**
  * Упаковщики исполняемых файлов (packers)
  * `UPX`
  * Ручная распаковка
  * Точка оригинального входа (OEP)
  * Код-заглушка (stub)
  * Дамп памяти
  * Восстановление таблицы импорта (IAT)
  * Отладка

**🔍 Что было сделано:**
* Разработана тестовая программа с различными функциями для демонстрации работы упаковщика
* Исследованы различные режимы упаковки UPX и их влияние на структуру файла
* Проведен детальный анализ процесса распаковки и выполнения UPX-заглушки
* Выполнена ручная распаковка файла с восстановлением всех необходимых структур
* Исследованы дополнительные упаковщики (MPRESS, ASPack) для сравнения техник

**🛠 Как было сделано:**
* Написана и скомпилирована программа hello.c с включением различных функций Windows API
* Использована утилита UPX с разными параметрами сжатия для упаковки бинарного файла
* Проанализирована структура упакованного файла с использованием PE-Bear и CFF Explorer
* Выполнена трассировка в x64dbg с установкой точек останова на ключевые участки кода
* Обнаружена точка оригинального входа (OEP) с использованием техники пошагового выполнения
* Создан дамп памяти процесса и восстановлена таблица импорта с помощью Scylla
* Проверена работоспособность восстановленного файла и сравнена с оригиналом

**📊 Результаты:**
* Успешно упакован и затем вручную распакован исполняемый файл с восстановлением функциональности
* Составлена детальная инструкция по процессу ручной распаковки UPX с иллюстрациями
* Выявлены особенности работы UPX и методы обнаружения упакованных файлов
* Созданы скрипты для автоматизации процесса распаковки в отладчике
* Подготовлена сравнительная таблица упаковщиков с их характеристиками и сложностью распаковки
* Предложены улучшения для существующих инструментов распаковки на основе полученного опыта

### Задание 7: Обфускация кода на C/C++

**🎯 Цель:** Изучить и применить техники обфускации кода для усложнения его чтения и анализа с целью защиты логики от реверс-инжиниринга.

**🛠️ Основные задачи:**
*   **Ручная обфускация:**
    *   Переименовать функции и переменные в бессмысленные имена.
    *   Заменить простые логические и арифметические операции на более сложные эквиваленты (например, используя битовые операции).
    *   Вставить "мертвый код" (dead code) — инструкции, которые не влияют на результат.
    *   Использовать макросы для усложнения чтения кода.
*   **Автоматическая обфускация:** Использовать `Obfuscator-LLVM` для применения техник обфускации на уровне промежуточного представления LLVM (замена операций, запутывание потока управления, добавление фиктивного потока управления).
*   Проанализировать результат обфускации: сравнить читаемость исходного кода и дизассемблированного кода до и после обфускации.

**🔑 Ключевые концепции:**
  * Обфускация кода
  * Защита интеллектуальной собственности
  * Переименование идентификаторов
  * Трансформация логики
  * Мертвый код
  * Макросы
  * `Obfuscator-LLVM`
  * Запутывание потока управления (Control Flow Flattening, Bogus Control Flow)

**🔍 Что было сделано:**
* Разработана программа с алгоритмом лицензионной проверки для демонстрации техник обфускации
* Применены различные методы ручной обфускации к исходному коду
* Настроен и использован Obfuscator-LLVM для продвинутой обфускации на уровне промежуточного представления
* Проведен сравнительный анализ эффективности различных методов обфускации
* Оценена устойчивость обфусцированного кода к реверс-инжинирингу

**🛠 Как было сделано:**
* Создан исходный код программы для защиты (`license_check.c`)
* Применены техники ручной обфускации:
  * Переименование переменных и функций в нечитаемые названия
  * Замена простых выражений сложными эквивалентами с битовыми операциями
  * Внедрение мертвого кода и фиктивных условных переходов
  * Использование макросов для маскировки логики
* Настроен и скомпилирован Obfuscator-LLVM с различными параметрами
* Применены продвинутые техники:
  * Сплющивание потока управления (Control Flow Flattening)
  * Подстановка фиктивных инструкций (Bogus Control Flow)
  * Обфускация строковых констант
* Проведена попытка реверс-инжиниринга обфусцированной программы для оценки эффективности

**📊 Результаты:**
* Создан набор программ с разными уровнями и типами обфускации
* Составлена сравнительная таблица эффективности различных методов обфускации
* Подготовлены исходные коды с комментариями, демонстрирующие применение каждой техники
* Разработаны рекомендации по выбору оптимальных методов обфускации для разных типов программ
* Оценено влияние обфускации на производительность программы и скорость компиляции
* Предложены комбинированные подходы к обфускации для достижения максимальной защиты

### Задание 8: Разработка простого эксплойта

**🎯 Цель:** Научиться находить уязвимости (переполнение буфера) в программе и писать простой эксплойт для эксплуатации этой уязвимости с целью получения контроля над потоком выполнения.

**🛠️ Основные задачи:**
1.  Создать уязвимую программу на C (`vuln.c`) с функцией `gets`, приводящей к переполнению буфера.
2.  Скомпилировать программу с отключенными защитами стека (`-fno-stack-protector`) и разрешенным исполнением на стеке (`-z execstack`) для 32-битной архитектуры.
3.  Проанализировать бинарный файл (`objdump`, `nm`) для нахождения адреса целевой функции (`secret`).
4.  Запустить программу под отладчиком (`gdb` с `pwndbg`) и вызвать переполнение буфера, чтобы определить смещение до адреса возврата (EIP).
5.  Написать скрипт на Python (`exploit.py`) для генерации полезной нагрузки (payload), состоящей из "мусорных" данных для заполнения буфера и перезаписи EIP адресом функции `secret`.
6.  Выполнить эксплойт, передав сгенерированную полезную нагрузку уязвимой программе, и получить шелл.
7.  Рассмотреть модификацию эксплойта и влияние защит (ASLR, canary).

**🔑 Ключевые концепции:**
  * Переполнение буфера (buffer overflow)
  * Эксплуатация уязвимостей
  * Адрес возврата (EIP/RIP)
  * Полезная нагрузка (payload)
  * Шелл-код (shellcode)
  * Отладка (`gdb`)
  * `pwndbg`
  * Защита стека (canary)
  * ASLR
  * `gets`

**🔍 Что было сделано:**
* Разработана уязвимая программа с несколькими векторами атаки
* Написан эксплойт для переполнения стека и перехвата управления
* Исследовано влияние различных защитных механизмов на возможность эксплуатации
* Разработаны методы обхода базовых защит (ASLR, NX-bit, Stack Canary)
* Создан демонстрационный стенд для безопасного тестирования эксплойтов

**🛠 Как было сделано:**
* Написана уязвимая программа `vuln.c` с использованием небезопасной функции `gets`
* Скомпилирована программа с отключенными защитами:
  ```
  gcc -m32 -fno-stack-protector -z execstack -o vuln vuln.c
  ```
* Проанализирован бинарный файл с помощью `objdump -d vuln` и `nm vuln` для поиска адресов функций
* Запущен отладчик `gdb` с плагином `pwndbg` для динамического анализа
* Определено точное смещение до адреса возврата путем отправки паттернов различной длины
* Написан Python-скрипт `exploit.py` с использованием библиотеки pwntools:
  ```python
  from pwn import *
  payload = b"A" * 76 + p32(0x080491e6)  # 76 байт + адрес функции secret()
  p = process("./vuln")
  p.sendline(payload)
  p.interactive()
  ```
* Протестированы различные виды полезной нагрузки, включая шелл-код для запуска оболочки
* Экспериментально определено влияние защит на успешность эксплуатации

**📊 Результаты:**
* Успешно разработан и протестирован эксплойт, позволяющий перехватить управление программой
* Создан набор скриптов для автоматизации процесса эксплуатации различных уязвимостей
* Подготовлена документация с пошаговым описанием процесса разработки эксплойта
* Разработаны методики обхода базовых защитных механизмов (ASLR, Stack Canary)
* Составлена таблица с анализом эффективности защитных механизмов и методов их обхода
* Подготовлена презентация для демонстрации работы эксплойта в образовательных целях

<hr/>

## 💡 Общий Вклад и Значимость

Представленный комплекс практических работ демонстрирует всестороннее освоение методик анализа, модификации и защиты программного обеспечения. Полученные навыки и знания являются фундаментом для дальнейших исследований и практической деятельности в области информационной безопасности и разработки защищенных систем, что полностью соответствует требованиям, предъявляемым к магистерской работе.

<hr/>

Автор: Шмарихин Дмитрий
Дата: 12.05.2025
